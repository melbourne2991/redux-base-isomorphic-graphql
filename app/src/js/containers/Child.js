import React, { Component, PropTypes } from 'react'

export default class Child extends Component {
  constructor(props) {
    super(props)
  }

  render() {
    return (
      <div>
        Child route!
      </div>
    )
  }
}